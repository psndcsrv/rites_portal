%p= link_to 'Add a class', new_portal_clazz_path(:teacher_id => teacher.id)
- teacher.clazzes.each do |c|
  = "#{c.name}" 
  = link_to 'Configure Offerings', edit_offerings_portal_clazz_path(c)
  = link_to 'View Class', portal_clazz_path(c)  
  = "Reports"
