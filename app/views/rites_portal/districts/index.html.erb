<h1>Listing districts</h1>

<table>
  <tr>
  </tr>

<% @districts.each do |district| %>
  <tr>
    <td><%= link_to 'Show', district %></td>
    <td><%= link_to 'Edit', edit_rites_portal_district_path(district) %></td>
    <td><%= link_to 'Destroy', district, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New district', new_rites_portal_district_path %>