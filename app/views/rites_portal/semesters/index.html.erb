<h1>Listing semesters</h1>

<table>
  <tr>
  </tr>

<% @semesters.each do |semester| %>
  <tr>
    <td><%= link_to 'Show', semester %></td>
    <td><%= link_to 'Edit', edit_rites_portal_semester_path(semester) %></td>
    <td><%= link_to 'Destroy', semester, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New semester', new_rites_portal_semester_path %>